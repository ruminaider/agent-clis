default_project_slug: gh/Recora-Health/recora-health-back-end

targets:
  auth:
    workflows:
      - build-and-deploy-auth
    jobs:
      - auth-tests
  care:
    workflows:
      - build-and-deploy-care
    jobs:
      - care-static-analysis
      - care-tests
  tonic:
    workflows:
      - build-and-deploy-tonic
    jobs:
      - tonic-static-analysis
      - tonic-tests
  agent-core:
    workflows:
      - build-and-deploy-agent-core
    jobs:
      - agent-core-checks
      - agent-core-eval-smoke
  all:
    workflows: []
    jobs: []
